# 学习进度（双机同步清单）

> 用途：实践周在两台电脑交替开发，本文件随仓库同步。换电脑后 `git pull`，打开本文件，从第一个未勾选项继续。
> 最后更新：2026-09-09

## 一、开发进度（共 12 步 + 收尾）

- [x] 第 1 步 数据层：`src/data/constants.js` + `seed.js`（已提交 bf9d025）
- [x] 第 2 步 工具层：`src/utils/` 四个文件（已提交 b2eb3f8）
- [x] 第 3 步 store 响应式状态层（`src/store/index.js`）——前置课：Vue课3 响应式
- [x] 第 4 步 路由守卫 + NavBar 登录态
- [x] 第 5 步 公共组件 StatusBadge / ItemCard / Pagination
- [x] 第 6 步 登录页 LoginView
- [x] 第 7 步 列表页 ItemsView
- [x] 第 8 步 详情页 DetailView
- [x] 第 9 步 发布页 PublishView（发布 + 编辑双模式）
- [x] 第 10 步 我的发布 MyPostsView
- [x] 第 11 步 首页 HomeView
- [x] 第 12 步 统计看板 StatsView（手写 SVG 图表）
- [ ] 收尾：完整回归走查 + 最终提交

## 二、知识学习进度（概念地图）

### JS 基础 ✅ 已学

const/let、箭头函数、对象、数组方法（map/find/some/filter）、模板字符串（反引号+${}）、三元表达式、import/export 两种写法、路径（./ 与 ../）、Object.keys、默认参数、try/catch

### Vue 概念课

- [x] Vue课1：`{{ }}` 插值、`v-bind`（冒号）、`v-on`（@）——自测：HomeView 加按钮实验
- [x] Vue课2：`v-if`/`v-else`、`v-for`、`:key`
- [x] Vue课3：`ref` / `reactive` / `computed`（第 3 步 store 的前置课）
- [ ] JS工具课：正则、Date 对象、JSON/localStorage、字符串方法（trim/slice/padStart）、Math.floor、逻辑运算符（&&、||、!、===）
- [x] Vue课4：组件通信 `props` / `emits` / `v-model`
- [x] Vue课5：路由参数、编程式导航
- [ ] Vue课6：生命周期 `onMounted`、`watch`（简）
- [ ] 配置文件扫尾：vite.config.js / .gitignore / eslint.config.js
- [ ] 练习课：用已学概念重读 data/ 和 utils/
- [ ] 练习课：views/ 7 个骨架页面

### HTML / CSS

- ✅ 已学：配对标签、class/id、语义标签（nav/main/section）、meta/title、HTML 注释、选择器（*、.class、标签名）、:hover、margin/padding 简写、box-sizing、十六进制与 rgba 颜色、flex 三件套 + gap、max-width + margin auto 居中、border-radius、box-shadow、文字样式（font/text-decoration）、scoped 与全局样式区别
- ⏳ 待学（写页面时现用现讲）：表单全家桶（form/input/select/textarea/label/button + placeholder/disabled/datetime-local）、:disabled/:focus、transition、cursor、text-align、line-height

### 全项目知识点账本

约 57 个知识点：已学约 33 个；剩余约 25 个全部排进上面的课（12 个 Vue 概念 + JS 工具课 10 来个 + 页面相关 CSS）。

## 三、换电脑继续的方法

1. 新电脑：`git pull`（若首次 clone 则先 `npm install`）
2. 打开本文件，从第一个未勾选的 [ ] 继续学习/开发
3. 每次完成一项，勾选并更新本文件，随代码一起提交
